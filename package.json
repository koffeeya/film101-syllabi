{
  "name": "film101-syllabi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vue-cli-service serve --open",
    "cleanup": "rm -rf .cache dist",
    "build": "vue-cli-service build --public-url https://koffeeya.github.io/film101-syllabi/",
    "deploy": "gh-pages -d dist",
    "push": "yarn cleanup && yarn build && yarn deploy"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "d3": "^6.7.0",
    "gh-pages": "^3.1.0",
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~4.5.0",
    "@vue/cli-service": "~4.5.0",
    "@vue/compiler-sfc": "^3.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}